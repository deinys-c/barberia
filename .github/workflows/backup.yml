name: Backup Gocho Barber

on:
  schedule:
    - cron: '0 12 * * 0'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar backup
        run: |
          curl -X GET "https://gochobarber.onrender.com/api/admin/backup-telegram?token=GochoBackup2026SeguroX8K9"
          echo "✅ Backup enviado"